Cloudflare Pages deployment settings

Build command:
npm install --cache .npm-cache && npm run build

Build output directory:
dist

Root directory:
(blank) / repository root

What was fixed:
- removed Base44-only Vite plugin for static hosting
- added Vite alias for @ -> src
- simplified App routing so it works without Base44 auth
- added missing UserNotRegisteredError re-export
- added public/_redirects for SPA routing on Cloudflare Pages

Recommended:
- Push this project to GitHub
- Create a Cloudflare Pages project from that repository
- Use the build settings above
